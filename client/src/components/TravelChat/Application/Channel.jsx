import React from "react"


export default function Channel(props) {

  return (
    <div></div>
  )

  // const channel = client.channel('messaging', 'the-small-council_fLlV4N61Jt', {
  //   name: "Private Chat About the Kingdom",
  //   image: "https://bit.ly/2F3KEoM",
  //   members: ["weathered-cake-0"],
  //   session: 8 // custom field, you can add as many as you want
  // });
  
  // await channel.watch();
  
  // return (
    
  //   channel
    
  //   );

  // const channel = chatClient.channel('messaging', 'custom_channel_id', {
  // add as many custom fields as you'd like
//   image: 'https://www.drupal.org/files/project-images/react.png', //logo
//   name: 'Talk about React',
//   members: ['little-tooth-2'],
// });

  // const { channel, setActiveChannel } = props;

  // const { messages } = channel.state;
  // const messagePreview = messages[messages.length - 1]?.text.slice(0, 30);

  // return (
  //   <div onClick={() => setActiveChannel(channel)} style={{ margin: '12px' }}>
  //     <div>{channel.data.name || 'Unnamed Channel'}</div>
  //     <div style={{ fontSize: '14px' }}>{messagePreview}</div>
  //   </div>
  // )
}